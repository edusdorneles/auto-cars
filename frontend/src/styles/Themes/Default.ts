import { createTheme } from "@mui/material";

const Default = createTheme({
    palette: {
        primary: {
            main: "#4184a4"
        },
        secondary: {
            main: "#3bb2e2"
        }
    }
});

export default Default;
